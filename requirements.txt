# Wearable Voice Translator v2 — Dependencies
# Core
requests>=2.31
python-dotenv>=1.0
numpy>=1.24
sounddevice>=0.4.6
Flask>=3.0

# Local TTS (primary — install on Raspberry Pi)
piper-tts>=1.2.0

# Cloud TTS fallback (for dev/testing on Windows, or when Piper model unavailable)
edge-tts>=6.1

# MP3 decoding (edge-tts returns MP3, we need WAV for playback)
miniaudio>=1.50

# Local STT (offline)
faster-whisper>=1.1

# Local translation (offline) — NLLB-200 via CTranslate2
ctranslate2>=4.0
sentencepiece>=0.2

# IndicF5 TTS (Indian regional languages)
# Install separately: pip install git+https://github.com/ai4bharat/IndicF5.git
